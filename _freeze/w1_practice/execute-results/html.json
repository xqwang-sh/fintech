{
  "hash": "0b050cbfcff156692df41f4436e71add",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: 第一讲上机实践\n---\n\n\n\n本上机讲义覆盖以下内容：\n\n**基础要求（必读）：**\n\n- 环境安装：Python + VS Code + 必要的库（pip）\n- 安装验证\n- Python 基础与机器学习第一个程序\n\n**进阶内容（可选）：**\n\n- Jupyter Notebook 的使用\n- 虚拟环境管理\n- uv 包管理工具\n\n## 基础环境安装\n\n### Step 1: 安装 Python（官方版本）\n\n**推荐方式：Python 官方安装包**\n\n1. **下载地址**：https://www.python.org/downloads\n2. **选择版本**：建议选择 3.10 - 3.12\n3. **安装步骤**：\n   - 下载适合你操作系统的安装程序\n   - 运行安装程序，**务必勾选 \"Add Python to PATH\"** ⚠️\n   - 点击\"Install Now\"或选择自定义安装\n4. **验证安装**：打开终端，运行：\n   ```bash\n   python --version\n   ```\n   显示版本号说明安装成功！\n\n**为什么不用 Anaconda？**\n\n- 占用空间大（超过1GB）\n- 容易出现包版本冲突\n- 对初学者更复杂\n\n### Step 2: 安装 VS Code\n\n1. **下载地址**：https://code.visualstudio.com/\n2. **安装步骤**：下载后按提示安装，然后启动 VS Code\n\n### Step 3: 安装 VS Code 插件\n\n打开 VS Code，按 `Ctrl+Shift+X` (Windows/Linux) 或 `Cmd+Shift+X` (Mac) 打开插件市场，搜索并安装：\n\n**必装插件：**\n\n1. **Python**（Microsoft 官方）- 代码支持和调试\n2. **Pylance** - 智能代码提示和类型检查  \n3. **GitHub Copilot**（推荐）- AI 代码助手\n   - **Student 优惠**：GitHub Student 账户免费享用 Copilot Pro（$10/月），申请地址：https://education.github.com/pack\n\n### Step 4: 安装必要的库\n\n打开终端，运行：\n```bash\npip install numpy pandas matplotlib scikit-learn\n```\n\n这会安装本课程需要的全部库：\n\n- `numpy` - 数值计算\n- `pandas` - 数据处理  \n- `matplotlib` - 数据可视化\n- `scikit-learn` - 机器学习\n\n**如果 pip install 出错？**\n\n**错误 1：`pip: command not found`**\n\n- 第1步：重启计算机，重新打开终端\n- 第2步：如果还是失败，手动添加 Python 到 PATH\n  - **Windows**：环境变量中添加 Python 安装路径\n  - **Mac/Linux**：在 `~/.zshrc` 中添加 `export PATH=\"/usr/local/bin:$PATH\"`\n- 第3步：如果依然失败，请重装Python，并确保勾选 \"Add Python to PATH\"\n\n**错误 2：网络问题**\n\n如果安装速度很慢或显示网络错误，可以尝试使用国内镜像源：\n\n```bash\npip install -i https://pypi.tuna.tsinghua.edu.cn/simple numpy pandas matplotlib scikit-learn\n```\n\n## 安装验证\n\n在终端验证：\n```bash\npython --version\npip --version\n```\n\n运行下面 Python 代码检查库是否安装：\n\n::: {#a7259011 .cell message='false' execution_count=2}\n``` {.python .cell-code}\n# 验证库安装情况\nlibs = ['numpy', 'pandas', 'matplotlib', 'sklearn']\n\nprint(\"正在验证库安装...\\n\")\nall_success = True\nfor lib in libs:\n    try:\n        __import__(lib)\n        print(f'✓ {lib} - 已安装')\n    except ImportError:\n        print(f'✗ {lib} - 未安装')\n        all_success = False\n\nif all_success:\n    print(\"\\n✓ 所有库已安装！环境配置完成 🎉\")\nelse:\n    print(\"\\n未安装的库需要运行：pip install numpy pandas matplotlib scikit-learn\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\n正在验证库安装...\n\n✓ numpy - 已安装\n✓ pandas - 已安装\n✓ matplotlib - 已安装\n✓ sklearn - 已安装\n\n✓ 所有库已安装！环境配置完成 🎉\n```\n:::\n:::\n\n\n## Python 基础：变量、列表、库\n\n### 变量与基本运算\n\n::: {#de397722 .cell message='false' execution_count=3}\n``` {.python .cell-code}\n# 变量赋值\nage = 20\nprice = 50.5\nname = \"Alice\"\nprint(age, price, name)\n\n# 基本运算\narea = 100              # 房屋面积（平米）\nprice_per_sqm = 5000   # 每平米价格（元）\ntotal_price = area * price_per_sqm\nprint(\"总价:\", total_price)\n```\n\n::: {.cell-output .cell-output-stdout}\n```\n20 50.5 Alice\n总价: 500000\n```\n:::\n:::\n\n\n**练习**：创建 `house_area = 120` 和 `price_per_sqm = 8000`，计算总价。\n\n### 列表\n\n::: {#0ed93f4f .cell message='false' execution_count=4}\n``` {.python .cell-code}\n# 创建列表\nhouse_prices = [100, 150, 200, 250, 300]\n\n# 访问元素\nprint(house_prices[0])    # 第一个元素\nprint(house_prices[-1])  # 最后一个元素\nprint(len(house_prices))  # 列表长度\n\n# 添加元素\nhouse_prices.append(350)\nprint(house_prices)\n\n# 遍历列表\nfor p in house_prices:\n    print(p)\n```\n\n::: {.cell-output .cell-output-stdout}\n```\n100\n300\n5\n[100, 150, 200, 250, 300, 350]\n100\n150\n200\n250\n300\n350\n```\n:::\n:::\n\n\n### 导入库与函数调用\n\n::: {#29accbb4 .cell message='false' execution_count=5}\n``` {.python .cell-code}\nimport numpy as np\nimport pandas as pd\n\n# numpy 示例\nscores = np.array([85, 90, 78, 92, 88])\nprint(\"平均分:\", np.mean(scores))\nprint(\"最高分:\", np.max(scores))\n\n# pandas 示例\ndata = {\n    '姓名': ['小明', '小红', '小刚'],\n    '成绩': [85, 90, 78]\n}\ndf = pd.DataFrame(data)\nprint(\"\\n学生成绩表:\")\nprint(df)\nprint(\"平均成绩:\", df['成绩'].mean())\n```\n\n::: {.cell-output .cell-output-stdout}\n```\n平均分: 86.6\n最高分: 92\n\n学生成绩表:\n   姓名  成绩\n0  小明  85\n1  小红  90\n2  小刚  78\n平均成绩: 84.33333333333333\n```\n:::\n:::\n\n\n## 第一个机器学习程序：线性回归\n\n**什么是机器学习？** 让计算机从数据中\"学习\"规律，然后用来做预测。\n\n**机器学习的基本流程：**\n1. 准备数据\n2. 数据探索与可视化\n3. 训练模型\n4. 预测新数据\n5. 评估模型\n\n::: {#fba7ef70 .cell message='false' execution_count=6}\n``` {.python .cell-code}\nfrom sklearn.linear_model import LinearRegression\nimport numpy as np\nimport matplotlib.pyplot as plt\n\n# ========== 第1步：导入库 ==========\n\n# ========== 第2步：准备数据 ==========\nareas = [50, 60, 70, 80, 90, 100, 110, 120]      # 房屋面积\nprices = [150, 180, 210, 240, 270, 300, 330, 360] # 房屋价格\n\n# ========== 第3步：数据探索与可视化 ==========\nplt.figure(figsize=(8, 5))\nplt.scatter(areas, prices, s=100, alpha=0.7, color='blue', label='真实数据点')\nplt.xlabel('房屋面积（平米）', fontsize=12)\nplt.ylabel('房屋价格（万元）', fontsize=12)\nplt.title('房屋面积与价格的关系', fontsize=14)\nplt.grid(True, alpha=0.3)\nplt.legend()\nplt.show()\nprint(\"观察：面积越大，价格越高，呈现线性关系 ✓\")\n\n# ========== 第4步：数据格式转换 ==========\nX = np.array(areas).reshape(-1, 1)\ny = np.array(prices)\n\n# ========== 第5步：创建和训练模型 ==========\nmodel = LinearRegression()\nmodel.fit(X, y)\nprint(\"\\n✓ 模型训练完成！\")\n\n# ========== 第6步：查看学到的参数 ==========\nprint(f\"学到的规律：价格 = {model.coef_[0]:.2f} × 面积 + {model.intercept_:.2f}\")\n\n# ========== 第7步：预测新数据 ==========\nnew_area = np.array([[95]])\npredicted = model.predict(new_area)[0]\nprint(f\"预测：95平米的房子价格约为 {predicted:.1f} 万元\")\n\n# ========== 第8步：评估模型 ==========\nplt.figure(figsize=(8, 5))\nplt.scatter(areas, prices, s=100, alpha=0.7, color='blue', label='真实数据点')\nplt.plot(areas, model.predict(X), color='red', linewidth=2, label='拟合直线')\nplt.scatter([95], [predicted], s=150, color='green', marker='*', label=f'预测：95平米 → {predicted:.1f}万元', zorder=5)\nplt.xlabel('房屋面积（平米）', fontsize=12)\nplt.ylabel('房屋价格（万元）', fontsize=12)\nplt.title('房价预测模型效果', fontsize=14)\nplt.grid(True, alpha=0.3)\nplt.legend(fontsize=10)\nplt.show()\nprint(\"✓ 模型效果评估完成！\")\n```\n\n::: {.cell-output .cell-output-display}\n![](w1_practice_files/figure-html/cell-7-output-1.png){width=663 height=453}\n:::\n\n::: {.cell-output .cell-output-stdout}\n```\n观察：面积越大，价格越高，呈现线性关系 ✓\n\n✓ 模型训练完成！\n学到的规律：价格 = 3.00 × 面积 + -0.00\n预测：95平米的房子价格约为 285.0 万元\n```\n:::\n\n::: {.cell-output .cell-output-display}\n![](w1_practice_files/figure-html/cell-7-output-3.png){width=663 height=453}\n:::\n\n::: {.cell-output .cell-output-stdout}\n```\n✓ 模型效果评估完成！\n```\n:::\n:::\n\n\n**恭喜！** 你已经完成了第一个机器学习程序！🎉\n\n---\n\n## 进阶内容（可选）\n\n### Jupyter Notebook（可选：交互式编程）\n\nJupyter 适合做数据分析和实验。两种方式：\n\n**方式 A：命令行方式**\n```bash\npip install jupyter notebook\njupyter notebook\n```\n\n**方式 B：VS Code 插件**\n\n- 在 VS Code 中安装 Jupyter 插件\n- 直接在 VS Code 中创建和运行 `.ipynb` 文件\n\n**本课程推荐使用 VS Code 编写 `.py` 文件**\n\n### 虚拟环境（可选：项目隔离）\n\n如果想为本课程单独建立编程环境（不影响系统）：\n\n```bash\n# 创建虚拟环境\npython -m venv venv\n\n# 激活虚拟环境\n# Windows:\nvenv\\Scripts\\activate\n# Mac/Linux:\nsource venv/bin/activate\n\n# 然后安装库\npip install numpy pandas matplotlib scikit-learn\n```\n\n**虚拟环境的好处：**\n\n- 库只安装在项目文件夹\n- 不同项目可以有不同的库版本\n- 项目便携性强\n\n**何时使用虚拟环境：**\n\n- 系统中已有其他版本的库 → 建议使用\n- 刚开始学 Python → 可直接安装到系统\n- 后续做多个项目 → 建议学习使用\n\n### uv 包管理工具（可选：更快的安装）\n\n`uv` 是一个现代的 Python 包管理工具，比 pip 快 10-100 倍。\n\n**安装和使用：**\n```bash\n# 安装 uv\npip install uv\n\n# 用 uv 安装库（两种方式都可以）\nuv pip install numpy pandas matplotlib scikit-learn\n# 或\nuv install numpy pandas matplotlib scikit-learn\n```\n\n**uv 的优势：**\n\n| 特性 | pip | uv |\n|------|-----|-----|\n| 安装速度 | 较慢 | ⚡ 快 10-100 倍 |\n| 依赖冲突 | 常有 | 更少 |\n| 初学者友好 | 一般 | ✓ 更好 |\n\n简单来说，`uv` 是\"自动帮你打理图书馆的管家\" ——让你不用担心安装细节，把注意力集中在代码本身。\n\n",
    "supporting": [
      "w1_practice_files/figure-html"
    ],
    "filters": [],
    "includes": {}
  }
}